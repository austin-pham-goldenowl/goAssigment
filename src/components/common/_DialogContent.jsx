import React from "react";
import DialogContent from "@material-ui/core/DialogContent";

const _DialogContent = ({ ...props }) => {
  return <DialogContent {...props} />;
};

export default _DialogContent;
