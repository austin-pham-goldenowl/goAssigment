import React from "react";
import DialogActions from "@material-ui/core/DialogActions";

const _DialogActions = ({ ...props }) => {
  return <DialogActions {...props} />;
};

export default _DialogActions;
